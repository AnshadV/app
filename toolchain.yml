# Github repos
 services:
  sample-repo:
    service_id: githubprivate
    parameters:
      repo_name: '{{toolchain.name}}'
      repo_url: 'https://github.com/AnshadV/app.git'
      type: clone
      has_issues: true
      enable_traceability: true
